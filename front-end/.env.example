# Base URL of the Laravel API, including the version prefix.
# Server-side only: the browser never talks to the backend directly.
API_URL=https://stands.test/api/v1

# Must match APP_TIMEZONE in the backend .env: it is what "today" means on the
# sale and receipt date fields.
BUSINESS_TIMEZONE=Africa/Harare
