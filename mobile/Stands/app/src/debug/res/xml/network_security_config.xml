<?xml version="1.0" encoding="utf-8"?>
<!--
    Development only. This file overlays the strict one in `src/main` for debug
    builds and is never packaged into a release.

    Two allowances, both needed to reach a backend running on the developer's
    own machine:

    - The loopback addresses may be spoken to in the clear, so the app can be
      pointed at a plain HTTP backend. Every other host still requires HTTPS,
      so this cannot quietly become a habit.
    - User-installed certificate authorities are trusted, so Herd's local CA can
      be added to the emulator and the app can use the same HTTPS URL the
      browser does.
-->
<network-security-config xmlns:tools="http://schemas.android.com/tools">
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <!-- Deliberate, and confined to this debug-only file. -->
            <certificates src="user" tools:ignore="AcceptsUserCertificates" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- The emulator's alias for the machine it is running on. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
